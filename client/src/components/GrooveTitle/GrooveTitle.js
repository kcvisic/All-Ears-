import React from "react";
import API from "../../utils/API";


const GrooveTitle = ({children}) => (

    <div className="titleWrapper text-center">
        <div>
            {children}
        </div>
    </div>

);

export default GrooveTitle;